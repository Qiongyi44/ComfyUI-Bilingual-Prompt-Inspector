# ComfyUI 双语提示词检查器：第一版至第十六版开发记录

> 本文件仅记录开发过程，早期界面、路径和安全说明可能已被后续版本替代。当前安装和使用方法以 `README.md` 为准。

一个独立、无模型依赖的 ComfyUI 自定义节点。它把英文提示词逐段显示为中英对应关系，并将输入英文逐字符原样输出。

## 第一版功能

- 标准多行英文输入，并提供扩展自身的翻译与提示词优化助手。
- 英文标签与中文解释逐项显示，悬停或点击时联动高亮。
- 支持逗号、换行、强调括号、数值权重和 `<...>` 特殊语法的非破坏性解析。
- 内置中英文基础词库，可按英文、中文、别名和分类搜索。
- 搜索结果只向提示词中插入英文。
- 添加和覆盖个人标签；个人词库优先于内置词库。
- 使用扩展自身的助手接口翻译未知标签，确认后才保存。
- 每条解释显示“内置词库、个人词库、检查器助手或语法保护”等来源。
- 自动拒绝明显扩写的机器译文，例如额外标签、额外权重、额外括号、多行内容或异常长度。
- 支持重新翻译，以及一键清除尚未保存的临时机器译文。
- 个人词库 JSON 导入和导出。
- 翻译、词库或前端面板失败时不影响英文透传和工作流生成。

## 安装

将整个目录放入 ComfyUI 的 `custom_nodes`，重启 ComfyUI，并刷新浏览器。节点位于：

`文本 / 提示词工具 / 双语提示词检查器（英文输出）`

## 数据安全

- `data/base_tags.json` 是只读内置词库。
- `data/user_tags.json` 是本机个人词库。
- 节点输出始终等于英文输入；中文解释不会进入模型条件。
- 扩展不修改 ComfyUI 或其他自定义节点，也不依赖其他提示词助手扩展。

## 独立助手

英文输入使用 ComfyUI 原生的 `STRING` 多行控件。检查器的文本翻译、标签解释和提示词优化全部通过自身的 `/bpi/assistant/*` 路由完成；安装、分享或使用本扩展时不需要额外安装提示词助手。

## 第二版功能

- 自动识别标签流、自然语言、`标签 + 末尾自然语言` 混合格式和“翻译要求 / 正文”附带指令，也可手动指定标签或自然语言模式。
- 自然语言按完整句子检查，避免把句子中的每个逗号误判为独立标签。
- 本地检查括号、权重、中文分隔符、空标签、重复标签和常见语义冲突；诊断只提示，不修改实际输出。
- 为每项显示解释来源与高、中、低可信度；机器翻译始终标记为低可信且待人工确认。
- 可在表格内直接修改中文解释，选择“仅本次”或保存到个人词库；保存个人解释不会修改内置词库文件。
- 中文或中英混合输入可进入绿色区域的“编辑文本”模式，再使用检查器自身的翻译功能处理。

### 自动模式的边界

自动识别采用轻量本地规则，不调用模型，因此不会占用显存。混合格式会把前面的短标签逐项检查，并把末尾完整句子保留为一个自然语言片段；遇到非常规输入时，仍可用检查器顶部的模式选择器强制指定“标签模式”或“自然语言”。

## 第三版：词库管理与快捷操作

- 独立词库管理窗口，可按英文、中文、别名、分类及来源搜索和筛选。
- 内置词库保持只读；编辑内置解释会创建个人覆盖，之后可一键删除覆盖并恢复内置解释。
- 支持批量确认待处理机器译、批量修改个人词条的分类和适用模型。
- 词库导入前显示新增、冲突、重复和无效数量，并列出不同中文解释。
- 导入冲突支持保留当前解释、使用导入解释，或把导入中文合并为别名。
- 每次导入和批量修改前自动备份 `data/user_tags.json` 到 `data/backups`。
- 收藏与最近插入记录保存在浏览器本地，不写入工作流，也不改变词库正文。
- 搜索按完全匹配、前缀、别名、包含关系和分类排序，收藏与最近使用用于同级排序。
- 搜索框支持上下方向键选择、回车插入、Ctrl+Enter 插入后保留搜索词、Esc 清空。
- 表格支持“全部、未知、机器译、个人词库、收藏”快捷筛选。
- 双击英文定位上方原文；双击中文直接编辑；每行可收藏、复制中英对照、删除个人词条或恢复内置解释。

## 第四版：独立词库包

- 内置词条拆分为基础常用、Anima、角色与作品、姿势、镜头构图、服装、成人内容七个独立包。
- 词库管理器显示每个包的启用状态、词条数量、版本、来源和许可信息。
- 每个内置或社区包可单独启用、停用和导出；停用只影响匹配，不删除文件。
- 个人词库不属于可停用包，始终启用，并在所有包合并完成后覆盖同名词条。
- 社区词库作为独立JSON包导入，不写入个人词库；同ID更新和删除前会自动备份。
- 旧 `data/base_tags.json` 原样保留；缺少包清单时自动退回兼容模式。

### 社区词库包格式

```json
{
  "schema_version": 1,
  "pack": {
    "id": "example-pack",
    "name": "示例社区词库",
    "version": "1.0.0",
    "source": "作者或社区名称",
    "license": "可选许可",
    "homepage": "可选来源页面"
  },
  "tags": [
    {
      "english": "example tag",
      "chinese": "示例标签",
      "aliases": ["示例别名"],
      "category": "示例分类",
      "models": ["general", "anima"],
      "verified": true
    }
  ]
}
```

如果导入文件只有 `tags`，管理器会要求填写包名、版本和来源，并将其保存为新的社区包。

## 第五版：精选词库扩充

内置词库已扩充到 `1.1.0`，共 1,073 个去重后的双语标签：

- 基础常用词库：239
- Anima 词库：28
- 角色与作品词库：189
- 姿势词库：136
- 镜头构图词库：166
- 服装词库：193
- 成人内容词库：122

词条来自 Anima/Comfy Org 官方资料、Danbooru 标签体系、Adobe/Nikon 摄影资料、HoYoverse 公开角色命名，以及经过筛选的 Civitai 社区案例。详细来源、可信度边界和成人词库排除范围见 [`data/packs/SOURCES.md`](data/packs/SOURCES.md)。

## 第六版：中英同步编辑

- 英文输入框下方新增逐标签中文组合视图；它只组合词库中的对应解释，不会再次调用机器翻译，也不会把中文传给模型。
- 点击中文标签、英文原文或下方管理行会联动高亮，并在英文输入框中选中同一处实际文本。
- 选中标签后可点击中文标签右侧的 `×`，或按 `Delete` / `Backspace`，同步删除当前英文标签；支持 `Ctrl+Z` 撤销和 `Ctrl+Y` 重做。
- 删除按具体出现位置处理，因此重复标签只删除当前选中的那一个；逗号、换行及相邻空白会同步整理。
- 未收录标签在中文组合视图中以警告色显示；点击后自动展开管理区、切换到“未知”筛选并定位对应项目，以便单独翻译和保存。
- 标签翻译与词库管理区可折叠，折叠状态保存在浏览器本地；收起时自动压缩节点并记住展开高度，重新展开时恢复原尺寸。
- 词库管理器中双击英文或中文可写入对应英文标签；右侧“插入”按钮保持单击执行，避免浏览词条时误触。

### 同步编辑边界

逐项删除在标签模式和混合模式的标签部分启用。自然语言片段和附带指令模式仍可查看中英对应关系，但不会通过中文组合视图逐词删除，以免误删句子内容。普通键盘编辑始终以英文输入框为准。

## 第七版：Danbooru 大型词库按需查询

- 大型词库保存在后端只读 SQLite 数据库中，不会把十几万或几十万条记录整体加载到浏览器。
- 标签模式或混合模式下输入提示词后，检查器会先使用个人词库和已启用的小型词库，再把未命中的英文标签合并为一次批量精确查询；混合模式末尾的自然语言片段不会送去查标签库。
- 搜索框和词库管理器支持按需查询大型词库，每次只返回少量结果，并使用约 300ms 防抖、会话命中缓存和未命中缓存降低磁盘访问。
- 个人词库始终最高优先级。修改大型词库的中文解释时，会新建个人覆盖，不改动 SQLite 原始记录。
- 词库管理器可独立启用或停用大型词库，并显示数量、版本、来源和“只读、按需查询”状态。
- 自然语言片段及附带指令模式不会逐词查询 Danbooru；需要时可通过检查器自己的“翻译”操作处理。

### 构建或更新本地大型词库

大型数据库不随公开扩展包捆绑。准备四列 TSV 文件（`name / category / post_count / cn_name`）后运行：

```powershell
python tools/build_large_dictionary.py "你的danbooru_zh.tsv路径"
```

生成文件为 `data/danbooru_tags.sqlite3`。替换数据库后重启 ComfyUI；个人词库不会受到影响。第三方翻译数据仅用于本地查询，授权范围以来源仓库说明为准。

## 第八版：文本草稿、独立助手与 Anima 顺序整理

- 绿色区域新增“编辑文本”，可在标签联动视图和中英混合文本编辑框之间切换。
- 文本翻译、优化与同步分开执行：处理结果先保留在绿色编辑框中，用户确认后再通过独立同步按钮写入实际英文输出。
- “按官方顺序整理”只在用户点击时执行。它先补查大型词库分类，再按照质量/元数据/年份/安全、人数、角色、作品/系列、画师、外观、其他标签、环境、自然语言描述九个字段稳定排序。
- 排序只移动标签，不改写内容；保留括号、权重、`@` 画师前缀和 `BREAK` / `AND` 分段。无法可靠判断的标签暂归“其他标签”，并在预览中显示数量。
- 翻译、优化和排序造成的整段替换均可通过 `Ctrl+Z` 撤销；普通生成流程仍只读取上方英文文本框。
- “助手设置”支持纯词库、Ollama 本地模型和 OpenAI 兼容 API，并可分别自定义“仅翻译”“翻译并优化”“优化为 Anima”三套规则和测试连接。
- API Key 仅由后端保存到 ComfyUI 用户目录，不写入工作流、不返回前端，也不会随扩展分享。

### 独立助手的默认行为

- 纯词库模式不需要模型，适合翻译已经收录的中文标签；无法识别的白话文会提示配置模型。
- Ollama 默认连接 `http://127.0.0.1:11434`，用户只需填写已经安装的模型名称。
- OpenAI 兼容模式需要填写 API 基础地址、模型名称和按服务商要求提供的 API Key。
- 所有模型调用只在用户主动按下翻译、优化、单项标签翻译或连接测试按钮时发生，不会随着键盘输入自动请求。

## 第九版：长提示词布局与联动显示

- 标签明细表固定在独立滚动区域内；标签数量增加时只滚动表格，不再持续拉长整个节点。
- 点击中文标签、英文标签或明细行后，中文标签、明细行和英文输入框会同时显示蓝色联动状态，并自动滚动到对应项目。
- 英文输入框可拖动右下角调整高度；调整结果保存在浏览器本地，刷新后继续沿用。
- 绿色文本框会随内容自动增高，到达上限后改为内部滚动，避免内容少时浪费空间或内容多时挤压其他操作区。

## 第十版：分阶段文本处理

- 顶部操作拆分为“仅翻译”“翻译并优化”“优化为 Anima”和“同步到英文输出”。任何翻译或优化结果都先留在绿色编辑框中，不会自动覆盖工作流实际输出。
- “仅翻译”自动判断方向：含中文的中文或混合文本翻译成英文，纯英文翻译成中文；不会附加优化内容。
- “翻译并优化”先处理语言，再整理为适合 Anima 的提示词；“优化为 Anima”仅接受英文输入。
- Anima 输出会过滤代码块和不兼容标点，并保留常用标签权重、角色名、画师前缀与自然语言所需符号。
- 绿色文本框根据内容自动调节高度，达到上限后显示滚动条。标签明细表继续使用独立滚动区域，长提示词不会无限拉长节点。
- 顶部文本操作、底部单项翻译及批量翻译统一使用检查器助手的 API、模型设置和规则，不再调用其他提示词插件。
- 三套默认规则可忠实处理明确为成年人的合法成人内容，不会主动淡化或扩写；涉及未成年人或年龄不明确对象的性化内容不会被处理为成人提示词。

## 第十一版：多节点状态同步

- 同一 ComfyUI 页面中的所有双语提示词检查器共享收藏和最近使用状态；任一节点修改收藏后，其他节点及已经打开的词库管理窗口会立即刷新。
- 临时机器翻译的待确认队列改为当前页面共享。一个节点产生、重译、清除或确认机器译后，其他节点的待确认筛选与数量会同步变化。
- 个人词库增删、机器译确认、导入及词库包启停会通知其他节点重新读取词库，避免不同节点继续显示旧缓存。
- 待确认机器译仍属于临时会话数据，不写入工作流；刷新整个网页后会清空。确认保存后的条目继续写入个人词库。

## 第十二版：滚轮事件隔离

- 鼠标位于标签表、搜索结果、绿色文本框、问题列表或词库管理器等可滚动区域时，滚轮只滚动当前区域，不再缩放 ComfyUI 画布。
- 滚轮在捕获阶段被识别并隔离，可覆盖 ComfyUI 画布先于节点面板处理滚轮的情况；滚动到区域顶部或底部时也不会意外缩放画布。
- 鼠标位于节点的普通非滚动区域时不拦截事件，仍可按 ComfyUI 原有方式缩放画布。

## 第十三版：中英文选区联动修复

- 单击或双击绿色区域中的中文标签时，会可靠地聚焦顶部英文输入框，并选中该标签对应的实际英文范围。
- 双击中文标签不再触发浏览器自身的中文文字选择，避免中文焦点覆盖英文选区。
- 英文文本框定位会排除检查器内部的绿色编辑框，兼容 ComfyUI 文本控件的直接元素与嵌套元素结构。

## 第十四版：标签与白话文边界识别

- 混合提示词不再依赖末尾句号判断白话文；检测到主语、谓语及自然语言连接结构后，即可确定末尾自然描述的起点。
- 白话文以逗号结尾时仍能识别为“标签 + 自然语言”，不会把整段提示词合并成一个未知项。
- 确定白话文起点后，其内部逗号全部保留在同一个自然语言片段中，不再拆成多个未知标签。
- 纯标签流末尾的多余逗号仍作为空标签提示；混合白话文末尾逗号不会产生误报警告。

## 第十五版：可见英文标签高亮

- 英文输入控件按照 ComfyUI 1.51 的真实文本控件索引定位，兼容 PrimeVue `textarea`、代理控件和旧式直接输入元素。
- 中文标签联动时除设置原生英文选区外，还会在对应字符范围上绘制独立蓝色高亮层，避免主题样式或焦点变化导致选区不可见。
- 高亮层同步英文框的字体、换行、滚动与尺寸，并且不接收鼠标事件，不影响输入、滚动和右下角按钮。

## 第十六版：英文双模式联动视图

- 顶部英文区不再依赖浏览器原生文本选区显示联动状态，而是提供“文本编辑”和“标签联动”两种视图；实际输出仍由隐藏的 ComfyUI 原生控件保存并传给工作流。
- 空提示词默认显示文本编辑框；输入期间保持编辑模式，失去焦点、按 `Ctrl+Enter` 或点击“完成编辑”后，再解析成可选择的英文标签。
- 已保存工作流中的非空提示词、中文区同步结果以及排序结果会直接显示为英文标签视图；点击“编辑文本”可随时返回普通文本框整体修改。
- 英文标签、绿色中文标签和底部明细行共享同一个选中状态。点击任意一处，其余两处会同步显示蓝色高亮并滚动到对应项目。
- 英文或中文标签被选中后可按 `Delete` / `Backspace` 删除，也可点击标签上的 `×`；删除、撤销、排序继续复用同一套文本解析与同步逻辑。
- 自然语言描述继续保持为一个完整块，不按内部逗号拆分，避免白话文被误删或破坏。
- 英文编辑框和英文标签区也纳入滚轮隔离，鼠标在可滚动内容上滚动时不会缩放 ComfyUI 画布。

## 第十七版：完整 ffdkj SQLite 适配与来源澄清

- 大型词库构建工具新增对 ffdkj 原始 `tag.sqlite` 的直接转换支持，同时保留旧版四列 TSV 兼容性；两种格式由文件内容自动识别。
- 转换前以只读模式打开来源 SQLite，执行完整性检查，并验证 `tags` 表包含 `name`、`category`、`cn_name`、`post_count` 四个必要字段。
- 转换时分批读取，自动生成规范化 `name_key`、中文分类、查询索引和来源元数据，不会把 30 多万条记录整体装入内存或浏览器。
- 输出替换采用临时文件和原子替换；已有数据库默认备份为 `danbooru_tags.sqlite3.previous`，转换失败不会破坏当前可用数据库。
- 元数据新增来源格式、原始行数、跳过行数和 SHA256，便于确认数据来源与更新版本。
- 公开说明区分“当前直接输入文件”和“上游数据来源”：旧测试库来自 Kohya-LoRA-Tool 的混合 TSV，新推荐方案由用户自行下载 ffdkj `tag.sqlite` 后在本地转换。
- 公开发布包继续排除原始数据库、转换后的数据库、旧库备份、个人词库内容和助手配置，不重新分发第三方标签数据。

## 第十八版：标签权重编辑与自然语言翻译校验修复

- 英文标签视图和绿色中文标签视图支持双击打开权重编辑器，自动写成 Anima 的 `(标签:权重)` 格式。
- 权重编辑器允许输入 `0–3`，提供 0.7、0.8、0.9、1、1.1、1.2、1.3、1.5 常用预设，并可一键清除已有权重。
- 权重修改按标签的具体位置执行，不会误改同名标签的其他出现位置；修改和清除均进入原有 Ctrl+Z / Ctrl+Y 历史。
- 自然语言片段翻译不再套用“单标签不得增加逗号”的防扩写限制，正常译文包含更多逗号时不会被误报为多个标签。
- 单标签翻译继续保留逗号、多行、额外权重及额外括号检查，避免模型把一个标签扩展为一串同义词或提示词。
- 调用助手翻译自然语言片段时附加“保持自然语言、不要拆成标签列表”的任务说明；默认翻译规则也明确区分单标签和自然语言。
- 默认规则只在用户仍使用第十七版原始默认值时自动升级；已经自行修改的翻译规则不会被覆盖。

## 第十九版：混合提示词双边界识别

- 混合模式从单一“白话起点”升级为“白话起点 + 白话终点”，支持标签后白话、白话后标签和“标签—白话—标签”三种排列。
- 自然语言内部逗号继续保留在同一个完整文本块中；白话结束后出现的连续标签恢复为独立、可联动、可删除、可改权重的标签。
- 后置标签边界通过连续短标签数量和质量、人数、安全等级、外观、风格、环境等显著信号联合判断，避免因一个短英文从句就武断拆分。
- 白话位于末尾且以逗号结束时继续保留末尾逗号，不产生空标签误报。
- 新增独立混合边界测试矩阵，包含 12 个定向用例与 27 个排列组合，共 39 轮；覆盖纯标签、纯白话、多逗号白话、短从句、标签后白话、白话后标签、标签夹白话、末尾逗号和不足证据不切分等结构。

### v19 Final 公开发布补充：关于作者

- 节点底部加入低干扰的“关于作者”入口，不挤占提示词编辑、词库与标签管理区域。
- 关于窗口中的“访问作者社区主页”指向固定 Bilibili 空间地址，并交由 ComfyUI/浏览器的外部链接机制打开。
- 链接不携带 `spm_id_from` 等跟踪参数，同时启用 `noopener`、`noreferrer` 和不发送来源页策略。
- 重写大型词库安装说明：统一采用把 `tag.sqlite` 临时放入扩展根目录的相对路径方案，分别提供 ComfyUI Desktop、Windows Portable、Git/整合包命令，并加入执行前双重 `Test-Path`、成功结果验证及无输出故障排查，避免盘符拼接和 Windows Store Python 空壳导致的静默失败。
