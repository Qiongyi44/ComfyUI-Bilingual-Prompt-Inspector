ComfyUI 双语提示词检查器 v1.1.0
================================

一、安装

1. 关闭 ComfyUI。
2. 解压“ComfyUI-Bilingual-Prompt-Inspector-v1.1.0-public.zip”。
3. 将解压得到的 ComfyUI-Bilingual-Prompt-Inspector 文件夹复制到：
   ComfyUI\custom_nodes\
4. 启动 ComfyUI。
5. 浏览器按 Ctrl+F5 强制刷新。
6. 在节点菜单中依次选择：
   文本 → 提示词工具 → 双语提示词检查器（英文输出）
7. 节点最底部的“关于作者”不会影响工作流；点击后可选择“访问作者社区主页”，由默认浏览器打开。

二、首次使用

1. 在顶部英文区域输入提示词。
2. 点击“完成编辑”，查看英文标签、绿色中文解释和底部明细。
3. 把节点右侧“英文提示词”连接到模型对应的文本编码节点。
4. 中文解释不会进入模型，只有英文实际输出会传递给工作流。
5. 在标签联动模式中双击英文或中文标签，可以设置或清除该标签的权重。

三、翻译助手

纯词库模式不需要 API。复杂文本可在“助手设置”中选择 Ollama、LM Studio 或 OpenAI 兼容 API。
API Key 不包含在分享包中。首次安装、完整卸载后重新安装、更换服务商或更换 API 地址时，都需要重新配置 Key。

四、大型词库

公开扩展包不包含大型 Danbooru 数据库。它是可选组件，不安装也不影响节点基本功能。
如需使用，推荐自行下载 ffdkj 仓库的 tag.sqlite，再使用扩展工具转换。不要直接改名覆盖。
完整下载、转换、更新及回退方法见压缩包外的“大型词库手动安装说明.txt”。

五、升级与重装

1. 关闭 ComfyUI，并建议先在词库管理器导出一次个人词库。
2. 不要删除旧扩展目录；把新版同名文件夹复制到 custom_nodes，选择合并并替换同名程序文件。
3. 不要使用会先清空目标目录的镜像同步工具，也不要“删除旧目录后解压”。
4. 正常覆盖会保留个人词库、社区词库、词库启停选择、大型数据库和安装身份。
5. 收藏和最近使用保存在浏览器本地存储中；使用相同浏览器、ComfyUI 域名和端口时会继续保留。
6. 更新后启动 ComfyUI，并按 Ctrl+F5 强制刷新浏览器缓存。

先删除整个扩展目录再安装属于重新安装，扩展目录内未导出的个人数据会丢失，旧 API 配置也可能失效。

六、安全提醒

不要把没有登录保护的 ComfyUI 直接开放到公网。
使用外部 API 时，提示词和规则会发送到用户自己选择的服务商。
详细功能、操作方法和故障排查请阅读扩展文件夹中的 README.md。

七、作者社区主页

https://space.bilibili.com/697555747
